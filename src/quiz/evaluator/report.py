"""평가 결과 리포트 생성 (HTML/Markdown)."""
from typing import Any

def generate_html_report(eval_results: dict[str, Any], output_path: str):
    """평가 결과를 시각화된 HTML 리포트로 변환합니다."""
    # TODO: HTML Template rendering
    print(f"Report generated at {output_path}")
